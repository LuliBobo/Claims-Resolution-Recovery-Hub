{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://claims-resolution-recovery-hub/schema/audit-event.schema.json",
  "title": "AuditEvent",
  "description": "Matches the live code/schema inspection in docs/migration/01_verified_inventory.md (2026-09-25). This is the only one of the 11 entities the inventory confirms has a creation timestamp (event_time). Append-only, historical evidence - never drop rows on migration.",
  "type": "object",
  "required": ["id", "linked_case_id", "event_time", "actor", "action"],
  "properties": {
    "id": { "type": "string" },
    "linked_case_id": { "type": "string", "description": "FK -> CustomerCase.id" },
    "event_time": { "type": "string", "format": "date-time" },
    "actor": { "type": "string" },
    "action": { "type": "string" },
    "previous_state": { "type": "string" },
    "new_state": { "type": "string" },
    "rule_source": { "type": "string" },
    "notes": { "type": "string" }
  }
}
